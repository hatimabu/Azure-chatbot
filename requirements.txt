# Simple requirements - only what you need to start!

# OpenAI Python library - to use ChatGPT
openai==1.12.0

# Python-dotenv - to load API key from .env file
python-dotenv==1.0.0

# That's it! Just 2 packages to start.
# As you grow, you'll add more:
# - Flask (for web interface)
# - SQLite (for database)
# - pandas (for data analysis)
# - numpy (for ML math)
# etc.